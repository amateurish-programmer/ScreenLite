# 开发记录

## 2026-09-17 · 准备
- 当前目录为空；Python 3.12 可用；已验证 GitHub 账号 amateurish-programmer。
- 完整 ChatGPT 对话读取遇到 Cloudflare，浏览器连接失败；已提供预览作为需求依据。
- 用户要求自动推进和阶段提交，执行时不重复请求确认。
- GitKraken 工具未提供，使用 Git CLI。现有 GPG 配置不可用；使用仅本仓库的 SSH 签名，不修改全局设置、不上传认证密钥。
- 采用 PySide6-Essentials 降低 Qt 模块数量；便携包使用 PyInstaller onedir，以便直接验证可运行产物。

## 阶段 1 完成
- HTML 规格、计划、Python 3.12 环境、私有远程仓库已建立。
- 配置恢复、尺寸换算与负坐标基础测试：6 passed。
- 使用仓库专用 SSH 提交签名；GitHub 未登记此签名公钥，因此不宣称 Verified。

## 阶段 2 完成
- 托盘、全局热键、设置、MSS 冻结截图、八点调整与 PNG/JPEG/WebP 异步导出实现。
- 本机显示器实测：1920x1080 物理像素，Qt 1536x864，缩放 125%，映射一致。
- 主窗口和图片窗口已渲染并目视检查；启动 smoke-test 正常退出。
- Qt 图片工作线程回调已明确 queued slots，修复剪贴板复制时崩溃；原生关闭选区已恢复空闲状态。
- 媒体后端并行完成并纳入本阶段基础，录屏流程在下一阶段接入。
- 完整测试 57 项通过，Ruff 通过（详见当前终端验证）。

## 阶段 3 完成
- 3 秒倒计时、Esc/热键取消、托盘录制计时、停止录制后导出 MP4 已接通。
- 已有视频压缩支持尺寸和画质预设、进度、取消及真实体积变化提示；取消转码清理未完成 MP4，异常/取消录制保留 MKV。
- 录制中退出会先正常停止并保留素材；已成功导出的本次录制才清理原始 MKV。
- 本机 320x240、15fps 真实 gdigrab 录制成功，13734 字节，FFmpeg 完整解码退出码 0。
- 修复无效保存路径丢失截图预览、图片 500x312 预览实际保存成 499x312 两项审查问题。
- 完整测试 67 项通过；Ruff 与 diff 检查通过。

## 完整方案补全阶段完成
- 后续重试成功取得完整对话，已存 conversation-source.md 并补入 HTML；原七阶段与现有工程阶段建立对应。
- 补齐截图自动复制/工具条，75%/25%/长边尺寸与锁比宽高；视频四档、比例、自定义范围；可配倒计时和鼠标。
- 补齐录制 HUD，捕获排除失败时隐藏；单屏录制前异步探测硬件/软件并回退。
- 本机真实探测选择 ddagrab-software；通过完整 Controller 录制成功、全文件解码成功。HUD 排除 API 返回 True，抽样画面为控制条后方背景。
- 测试 95 项通过；更新窗口与工具条预览已目视检查。
- 打包发现开发环境 PATH 带入不兼容 ICU DLL，已限定搜索路径；全新诊断构建启动/退出通过，最终包待重建。

## 阶段 4 完成
- 已生成 PyInstaller onedir 便携 ZIP，约72.6 MiB，解压约191 MiB，附 SHA256、许可证与使用文档。
- 最终 EXE help/启动退出检查通过；包含热键的检查退出码0、日志无错误。
- 最终测试96项通过，Ruff和diff检查通过。ZIP CRC和运行数据排除检查通过。
- 包内所有业务模块与当前源码递归代码对象一致，已确认最后保存失败保留截图修复进入包。
- 本机验收与未覆盖硬件矩阵见 ACCEPTANCE.md；GitHub CI配置随阶段提交，远程结果单独核对。
