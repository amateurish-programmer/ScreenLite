# ScreenLite 验收记录

## 0.2.0 体验修订（2026-09-17）

- 完整回归：126 项通过；Ruff 与 `git diff --check` 通过。
- 新增标注/选区物理像素、125%/150% 合成图、撤销重做、跟随定位、文字预览对比度和样式同步验证。
- 启动显示、最小化保留托盘、关闭请求退出、窗口与大图资源生命周期已有回归验证。
- 两轮独立审查已闭环；Qt 合成工具条截图已目视检查。
- 4K 合成图片工作流中，导出阶段专用内存中位数 152.21 → 68.30 MiB；100 轮循环验证和工作集差异见 [内存测量](MEMORY.md)。
- 新版 EXE `--help`、`--smoke-test --no-hotkeys` 通过；原生窗口检查连跑两次：首次与已有配置均显示主界面，启动 2.56 / 1.47 秒；最小化后进程存活且两组快捷键仍注册；主窗口关闭后退出码 0，两组快捷键均释放。运行日志为空。
- 产物：`dist/v0.2.0/ScreenLite-0.2.0-windows-x64.zip`，约 72.6 MiB；同目录 SHA256 校验文件。原生窗口验证脚本为 `scripts/check_portable_window.py`。
- 包内 14 个业务模块与当前源码递归代码对象一致；ZIP CRC 检查通过，排除运行数据和用户素材。
- 新增功能尚未实测：多显示器混合 DPI、原生中文输入法候选窗与长时间高频标注操作。

## 0.1.0 历史验收（以下为首版记录）

日期：2026-09-17。环境：Windows 10 22H2 / 19045，x64，单显示器 1920×1080，125% 缩放，Python 3.12.12。

## 已通过

- `python -m pytest -q`：96 项，包括坐标、配置恢复、热键解析、UI 操作、异步线程、导出尺寸、格式、源文件保护、真实合成视频转码、取消、录制收尾与探测回退。
- `python -m ruff check src tests scripts`、`git diff --check`。
- 本机显示器映射：Qt 1536×864，MSS 1920×1080，125% 坐标换算一致。
- 真实 `gdigrab` 320×240、15fps 录制，完整视频解码退出码 0。
- 真实硬件编码探测失败后自动选择 `ddagrab-software`；通过应用 Controller 录制并完整解码，退出码 0。
- Windows 接受录制 HUD 的 `WDA_EXCLUDEFROMCAPTURE`；录制帧中控制条覆盖位置抽样显示背景色。
- 主窗口、图片导出、视频导出、截图工具条生成实际 Qt 预览并目视检查。
- 打包后 EXE `--help`、`--smoke-test --no-hotkeys`、`--smoke-test` 均正常退出。最后一次含热键初始化的启动/退出耗时 3.45 秒（包含 1 秒自测定时等待）。
- ZIP CRC 检查通过；没有运行时 `data/`、用户导出或测试桌面素材。
- 对包内 Python 模块与当前源码逐模块比较递归代码对象，全部一致，确认最后的保存失败修复已进入程序包。

## 产物

`dist/ScreenLite-0.1.0-windows-x64.zip`，约 72.6 MiB；解压约 191 MiB。
同目录 `.sha256` 文件给出最终校验值。解压运行 `ScreenLite/ScreenLite.exe`。
Python/Qt/FFmpeg 包体不属于几 MB 的原生小程序；空闲时没有录屏进程、逐帧 Python 循环或后台轮询任务。

## 明确未验收

- 多显示器混合 100/125/150/175/200% DPI、负坐标实机选区（负坐标计算已有单元测试）。
- 2K/4K、长时 30/60fps、内存增长和持续 CPU 占用基准。
- Windows 11、睡眠/锁屏、显示器热插拔、远程桌面与其它显卡组合。
- 首版不录音，不含 OCR、标注、滚动截图、GIF、暂停；这些遵循原规划的首版边界。
- 深色模式、开机启动属于原文可选建议，当前未加入。多显示器自动 DXGI 映射未核验，使用兼容录制路径。
- 正式对外二进制分发的完整第三方对应源码链尚未核验，本包用于本地评估；详见 THIRD_PARTY.md。

## 修复与追溯

测试发现并修复了 Qt 图片线程回调崩溃、原生关闭选区卡住、取消转码残留、预览与输出差 1 像素、无效保存路径丢图和失败保存后的待关闭丢图。
打包时发现开发环境的 Poppler ICU DLL 被误收集，限制构建 PATH 后重新构建，独立 EXE 启动通过。
完整对话在初次读取受阻后重试取得，补全需求另有阶段提交，历史记录见 progress.md。
